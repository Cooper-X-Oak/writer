/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: Pinned to build-independent references so CI `tsc --noEmit` passes without first running
// `next build`. A local `next build` will append a `.next/types/...` reference — leave that
// uncommitted. See https://nextjs.org/docs/app/api-reference/config/typescript
