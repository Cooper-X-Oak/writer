// @app/web — Next.js App Router frontend (collect / write / preview / export).
//
// Placeholder. P0-4 turns this into a Next.js app that calls the daemon /api/health through
// lib/api using @app/contracts types only. See docs/issues-phase-0-1.md.

export const WEB_PACKAGE = '@app/web';
